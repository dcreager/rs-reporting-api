[package]
name = "reporting-api"
version = "0.1.0"
authors = ["Douglas Creager <dcreager@dcreager.net>"]
edition = "2018"

[dependencies]
serde = { version="^1.0", features=["derive"] }
serde_json = "^1.0"
typetag = "^0.1"
